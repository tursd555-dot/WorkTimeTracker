# 🔍 Как найти Supabase Keep-Alive workflow на GitHub

## Проблема
GitHub Actions показывает "Get started" потому что смотрит на main ветку, а наш workflow в ветке `claude/supabase-activity-scheduler-TTPzf`.

## Решение: 2 варианта

### ✅ Вариант 1: Просмотр workflow в ветке (БЫСТРО)

1. **Переключите ветку на GitHub:**
   - Вверху слева на странице найдите кнопку с названием ветки (обычно "main")
   - Кликните на нее
   - Выберите `claude/supabase-activity-scheduler-TTPzf`

2. **Перейдите в Actions снова:**
   - После переключения ветки, кликните **Actions** вверху

3. **Workflow должен появиться:**
   - Слева должен быть "Supabase Keep-Alive"
   - Если нет - кликните "set up a workflow yourself" → и вы увидите что файл уже существует

### ✅ Вариант 2: Проверка файла напрямую (ЕЩЕ БЫСТРЕЕ)

Просто откройте этот URL в браузере:

```
https://github.com/tursd555-dot/WorkTimeTracker/blob/claude/supabase-activity-scheduler-TTPzf/.github/workflows/supabase-keepalive.yml
```

Вы должны увидеть содержимое workflow файла.

### ✅ Вариант 3: Запуск из ветки

1. На GitHub откройте:
   ```
   https://github.com/tursd555-dot/WorkTimeTracker/actions
   ```

2. Если переключили на ветку `claude/supabase-activity-scheduler-TTPzf`:
   - Слева будет "Supabase Keep-Alive"
   - Кликните на него
   - Справа будет кнопка "Run workflow"

### ⚡ Вариант 4: Мердж в main (РЕКОМЕНДУЕТСЯ)

Чтобы workflow был виден всегда и работал автоматически:

1. **Создайте Pull Request:**
   ```
   https://github.com/tursd555-dot/WorkTimeTracker/pull/new/claude/supabase-activity-scheduler-TTPzf
   ```

2. **Или через CLI:**
   ```bash
   # Если есть gh CLI
   gh pr create --title "Add Supabase Keep-Alive automation" --body "Автоматическое поддержание активности Supabase на Free Tier"
   ```

3. **После мерджа в main:**
   - Workflow станет видимым всегда
   - Автоматические запуски будут работать по расписанию

---

## 🎯 Что делать прямо сейчас:

**Самый простой способ - откройте прямую ссылку:**

```
https://github.com/tursd555-dot/WorkTimeTracker/blob/claude/supabase-activity-scheduler-TTPzf/.github/workflows/supabase-keepalive.yml
```

Это покажет что файл существует. Затем вернитесь в Actions и переключите ветку.

---

Попробуйте открыть эту ссылку, и дайте знать что видите!
