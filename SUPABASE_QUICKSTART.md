# 🚀 Быстрый старт с Supabase

## ✅ Исправление выполнено

Проблема с требованием Google Credentials в Supabase режиме **исправлена**.

## 📝 Что было сделано

1. **Исправлен порядок загрузки конфигурации**
   - `.env` файл теперь загружается ДО инициализации Supabase
   - Переменные окружения из `.env` имеют приоритет

2. **Добавлена условная валидация**
   - Если `USE_SUPABASE=True` → проверяются только Supabase credentials
   - Если `USE_SUPABASE=False` → проверяются только Google Sheets credentials

3. **Оптимизирован импорт библиотек**
   - `cryptography` импортируется только когда нужен (Google Sheets режим)
   - Избегаем ошибок с системными пакетами

4. **Создан .env файл**
   - Базовая конфигурация для Supabase режима
   - Все необходимые переменные окружения

## 🔧 Настройка проекта

### 1. Проверьте .env файл

Файл `.env` уже создан в корне проекта. Проверьте следующие параметры:

```bash
# Supabase (уже настроено)
SUPABASE_URL=https://jtgaobxbwibjcvasefzi.supabase.co
SUPABASE_KEY=eyJhbGci...

# Telegram (НУЖНО НАСТРОИТЬ!)
TELEGRAM_TOKEN=your_bot_token_here          # Замените на ваш токен от @BotFather
TELEGRAM_MONITORING_CHAT_ID=your_chat_id_here  # Замените на ваш chat ID
```

### 2. Получите Telegram токен (если еще нет)

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям, получите токен
4. Скопируйте токен в `.env` файл (переменная `TELEGRAM_TOKEN`)

### 3. Узнайте ваш Chat ID

1. Напишите вашему боту `/start`
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
3. Найдите `"chat":{"id":123456789}` - это ваш Chat ID
4. Скопируйте в `.env` файл (переменная `TELEGRAM_MONITORING_CHAT_ID`)

### 4. Установите зависимости

```bash
# В Windows PowerShell:
pip install -r requirements.txt
pip install supabase python-dotenv

# Или установите вручную основные пакеты:
pip install PyQt5 supabase python-dotenv pyzipper
```

### 5. Запустите приложение

```powershell
# Пользовательское приложение
python user_app/main.py

# Или админка
python admin_app/main_admin.py

# Или Telegram бот
python telegram_bot/main.py
```

## ✅ Проверка конфигурации

Запустите быструю проверку:

```powershell
python -c "import config; print('✓ Config OK')"
```

**Ожидаемый вывод:**
```
✅ Использование: Supabase
✓ Конфигурация успешно проверена
✓ Стратегия повторных попыток: [60, 300, 900, 1800, 3600]
✓ Мониторинг chat ID: your_chat_id_here
✓ Лимиты: Перерыв=45мин, Обед=60мин
✓ Config OK
```

## 🔄 Миграция данных из Google Sheets (опционально)

Если у вас уже есть данные в Google Sheets:

```powershell
python migrate_to_supabase.py
```

Это перенесет все данные из Google Sheets в Supabase.

## 🐛 Возможные проблемы

### Проблема: "SUPABASE_URL не задан"

**Решение:** Убедитесь что `.env` файл находится в корне проекта (`D:\proj vs code\WorkTimeTracker\.env`)

### Проблема: "Module not found: supabase"

**Решение:**
```powershell
pip install supabase
```

### Проблема: "Module not found: dotenv"

**Решение:**
```powershell
pip install python-dotenv
```

### Проблема: Приложение не видит .env файл

**Решение:** Запускайте из корня проекта:
```powershell
cd "D:\proj vs code\WorkTimeTracker"
python user_app/main.py
```

## 📊 Преимущества Supabase

После настройки вы получите:

- ⚡ **30x быстрее** - запросы выполняются за 0.05 сек вместо 1.5 сек
- 🚀 **Больше лимитов** - 50,000 запросов/день вместо 100/100сек
- 💪 **Масштабируемость** - до 1000+ пользователей
- 💰 **Бесплатно** - Free Tier полностью достаточен
- 🔄 **Real-time** - мгновенные обновления

## 📚 Дополнительная документация

- `SUPABASE_MIGRATION_GUIDE.md` - Полное руководство по миграции
- `README.md` - Общая документация проекта
- `.env.example` - Пример конфигурации (если нужно пересоздать .env)

## 🎉 Готово!

Теперь приложение должно запускаться без ошибок в Supabase режиме.

---

**Вопросы?** Проверьте логи в:
- Windows: `%APPDATA%/WorkTimeTracker/logs/`
- Linux/Mac: `~/.local/share/WorkTimeTracker/logs/`
