# 🚀 WorkTimeTracker - Миграция на Supabase

**Полное руководство по переходу с Google Sheets на Supabase**

---

## 📋 **СОДЕРЖАНИЕ:**

1. [Зачем мигрировать?](#зачем-мигрировать)
2. [Подготовка](#подготовка)
3. [Создание проекта Supabase](#создание-проекта-supabase)
4. [Миграция данных](#миграция-данных)
5. [Обновление приложений](#обновление-приложений)
6. [Настройка синхронизации](#настройка-синхронизации)
7. [Тестирование](#тестирование)
8. [Откат (если нужно)](#откат)

---

## 🎯 **ЗАЧЕМ МИГРИРОВАТЬ?**

### **Проблемы Google Sheets:**
- ❌ Лимит API: 100 запросов / 100 секунд
- ❌ Медленные запросы: 1-3 секунды
- ❌ Частые превышения лимита
- ❌ Нет транзакций
- ❌ Нет индексов
- ❌ Проблемы с конкуренцией

### **Преимущества Supabase:**
- ✅ **30x быстрее** (0.05 сек vs 1.5 сек)
- ✅ **50,000 запросов/день** (бесплатно!)
- ✅ **Real-time обновления**
- ✅ **PostgreSQL** (транзакции, индексы)
- ✅ **Масштабируемость** до 1000+ пользователей
- ✅ **$0/месяц** (бесплатный тариф)

### **Итого:**
| Параметр | Google Sheets | Supabase |
|----------|---------------|----------|
| Скорость | 1.5 сек | 0.05 сек |
| Лимит API | 100/100сек | 50,000/день |
| Стоимость | $0 | $0 |
| Масштаб | 50 польз. | 1000+ польз. |

---

## 📦 **ПОДГОТОВКА:**

### **1. Установите зависимости:**

```powershell
pip install supabase --break-system-packages
```

### **2. Файлы миграции:**

Скачайте:
- ✅ `supabase_schema.sql` - SQL схема БД
- ✅ `supabase_api.py` - Python модуль
- ✅ `migrate_to_supabase.py` - Скрипт миграции
- ✅ `sync_to_sheets.py` - Синхронизация отчетов

### **3. Backup:**

```powershell
# Экспорт Google Sheets в Excel
# Или автоматический backup при миграции
```

---

## 🌐 **СОЗДАНИЕ ПРОЕКТА SUPABASE:**

### **Шаг 1: Регистрация**

1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Войдите через GitHub (или email)

### **Шаг 2: Создание проекта**

1. Нажмите "New Project"
2. Заполните:
   - **Name:** `worktimetracker`
   - **Database Password:** (надежный пароль, сохраните!)
   - **Region:** `Europe West (Frankfurt)` (ближайший)
3. Нажмите "Create new project"
4. Подождите 2-3 минуты

### **Шаг 3: Получение credentials**

1. В меню слева: **Settings** → **API**
2. Скопируйте:
   - **URL:** `https://xxxxxx.supabase.co`
   - **anon public key:** `eyJh...` (длинный ключ)

### **Шаг 4: Создание схемы БД**

1. В меню слева: **SQL Editor**
2. Нажмите "+ New query"
3. Скопируйте содержимое `supabase_schema.sql`
4. Вставьте в редактор
5. Нажмите **RUN** (или Ctrl+Enter)
6. Дождитесь: `Success. No rows returned`

✅ **База данных готова!**

---

## 🔧 **НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ:**

### **Windows (PowerShell):**

```powershell
# Временно (для текущей сессии)
$env:SUPABASE_URL="https://your-project.supabase.co"
$env:SUPABASE_KEY="your-anon-key"

# Постоянно (системные переменные)
[System.Environment]::SetEnvironmentVariable("SUPABASE_URL", "https://your-project.supabase.co", "User")
[System.Environment]::SetEnvironmentVariable("SUPABASE_KEY", "your-anon-key", "User")
```

### **Или файл .env:**

Создайте файл `.env` в корне проекта:

```
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-key
```

Установите `python-dotenv`:

```powershell
pip install python-dotenv --break-system-packages
```

---

## 📊 **МИГРАЦИЯ ДАННЫХ:**

### **Шаг 1: Запуск миграции**

```powershell
cd "D:\proj vs code\WorkTimeTracker"
python migrate_to_supabase.py
```

### **Шаг 2: Процесс миграции**

Скрипт выполнит:

1. ✅ **Подключение к Supabase**
2. ✅ **Backup данных** из Google Sheets
3. ✅ **Миграция пользователей**
4. ✅ **Миграция WorkLog**
5. ✅ **Миграция BreakLog**
6. ✅ **Миграция Violations**
7. ✅ **Проверка миграции**
8. ✅ **Итоговая статистика**

**Ожидаемое время:** 10-30 минут (зависит от объема данных)

### **Пример вывода:**

```
================================================================================
🚀 МИГРАЦИЯ WORKTIMETRACKER
   Google Sheets → Supabase
================================================================================

🔌 ПОДКЛЮЧЕНИЕ К SUPABASE
✅ Подключено к: https://xxxxxx.supabase.co

📦 BACKUP ДАННЫХ ИЗ GOOGLE SHEETS
📋 Читаем Users...
   ✅ 150 пользователей
📋 Читаем WorkLog...
   ✅ 5000 записей
...

👥 МИГРАЦИЯ ПОЛЬЗОВАТЕЛЕЙ
Всего пользователей: 150
   Обработано: 10/150
   Обработано: 20/150
   ...
✅ Мигрировано: 150/150

📊 МИГРАЦИЯ WORKLOG
Всего записей: 5000
   Batch 1/50: 100/5000
   Batch 2/50: 200/5000
   ...
✅ Мигрировано: 5000/5000

📊 ИТОГОВАЯ СТАТИСТИКА
Таблица              Всего      Успешно    Ошибки    
--------------------------------------------------
users                150        150        0         
work_log             5000       5000       0         
break_log            200        200        0         
violations           50         50         0         
--------------------------------------------------
ИТОГО:                          5400       0         

🎉 МИГРАЦИЯ ЗАВЕРШЕНА УСПЕШНО!
```

---

## 🔄 **ОБНОВЛЕНИЕ ПРИЛОЖЕНИЙ:**

### **Вариант 1: Использовать supabase_api.py**

Замените в коде:

```python
# БЫЛО:
from sheets_api import get_sheets_api
api = get_sheets_api()

# СТАЛО:
from supabase_api import get_supabase_api
api = get_supabase_api()
```

**Преимущества:**
- Совместимый интерфейс
- Минимальные изменения
- Все методы работают

**Недостатки:**
- Нужно обновить все файлы

---

### **Вариант 2: Compatibility Wrapper**

Добавьте в начало `config.py`:

```python
# Переключение на Supabase (v20.5)
USE_SUPABASE = True  # True = Supabase, False = Google Sheets

if USE_SUPABASE:
    from supabase_api import SheetsCompatibilityAPI as SheetsAPI
else:
    from sheets_api import SheetsAPI
```

**Преимущества:**
- Одно место переключения
- Легко откатить
- Нет изменений в коде приложений

**Недостатки:**
- Дополнительный слой абстракции

---

## 📅 **НАСТРОЙКА СИНХРОНИЗАЦИИ С GOOGLE SHEETS:**

Для отчетов и визуального просмотра:

### **Ручной запуск:**

```powershell
python sync_to_sheets.py
```

### **Автоматический запуск (Windows Task Scheduler):**

1. Откройте **Планировщик заданий**
2. Создать задачу:
   - **Имя:** `WorkTimeTracker Sync`
   - **Триггер:** Ежедневно в 23:00
   - **Действие:** 
     ```
     Программа: python
     Аргументы: D:\proj vs code\WorkTimeTracker\sync_to_sheets.py
     ```
3. Сохранить

**Результат:** Каждый день в 23:00 данные из Supabase экспортируются в Google Sheets для отчетов.

---

## ✅ **ТЕСТИРОВАНИЕ:**

### **Тест 1: Подключение**

```python
from supabase_api import get_supabase_api

api = get_supabase_api()
users = api.get_users()
print(f"Пользователей: {len(users)}")
# Ожидаемый вывод: Пользователей: 150
```

### **Тест 2: Скорость**

```python
import time
from supabase_api import get_supabase_api

api = get_supabase_api()

start = time.time()
users = api.get_users()
duration = time.time() - start

print(f"Загрузка {len(users)} пользователей: {duration:.3f} сек")
# Ожидаемый вывод: ~0.050 сек (вместо 1.5 сек!)
```

### **Тест 3: User App**

1. Запустите User App
2. Login
3. Проверьте что статусы меняются
4. Поставьте перерыв
5. Проверьте в Supabase Dashboard что данные записались

### **Тест 4: Admin App**

1. Запустите Admin App
2. Откройте вкладку "Аналитика"
3. Dashboard должен загрузиться мгновенно (< 1 сек)
4. Проверьте что видны активные сессии и перерывы

### **Тест 5: Синхронизация**

1. Запустите `python sync_to_sheets.py`
2. Откройте Google Sheets
3. Проверьте что листы обновились:
   - Users
   - ActiveSessions
   - WorkLog (последние 7 дней)
   - BreakLog (последние 7 дней)
   - Violations (последние 30 дней)
   - DailyStats

---

## 🔄 **ОТКАТ (если нужно):**

Если что-то пошло не так:

### **Шаг 1: Остановите приложения**

Закройте User App и Admin App.

### **Шаг 2: Переключите обратно на Google Sheets**

В `config.py`:

```python
USE_SUPABASE = False  # Вернуться на Google Sheets
```

### **Шаг 3: Восстановите данные (если нужно)**

Backup создан автоматически:

```powershell
# Найдите файл backup_sheets_YYYYMMDD_HHMMSS.json
# Импортируйте обратно в Google Sheets (если нужно)
```

### **Шаг 4: Запустите приложения**

```powershell
run_user.bat
run_admin.bat
```

✅ **Откат завершен!**

---

## 📊 **МОНИТОРИНГ:**

### **Supabase Dashboard:**

https://app.supabase.com

- **Table Editor** - просмотр данных
- **SQL Editor** - выполнение запросов
- **Logs** - логи API запросов
- **Database** - метрики БД

### **Логи синхронизации:**

Таблица `sync_log` в Supabase:

```sql
SELECT * FROM sync_log 
ORDER BY started_at DESC 
LIMIT 10;
```

---

## 💰 **ТАРИФЫ SUPABASE:**

### **Free Tier (достаточно для 200 пользователей):**
- ✅ 500 MB база данных
- ✅ 50,000 API запросов/день
- ✅ 1 GB хранилища
- ✅ 2 GB bandwidth/месяц
- ✅ Real-time subscriptions
- ✅ Edge Functions

### **Pro ($25/месяц - если нужно больше):**
- ✅ 8 GB база данных
- ✅ 500,000 API запросов/день
- ✅ 100 GB хранилища
- ✅ 50 GB bandwidth/месяц
- ✅ Daily backups

**Для 200 пользователей Free Tier более чем достаточно!**

---

## 📝 **CHECKLIST МИГРАЦИИ:**

- [ ] Создан проект Supabase
- [ ] Выполнен `supabase_schema.sql`
- [ ] Настроены переменные окружения
- [ ] Установлена библиотека `supabase`
- [ ] Создан backup данных
- [ ] Выполнена миграция (`migrate_to_supabase.py`)
- [ ] Проверено количество записей
- [ ] Обновлены приложения на `supabase_api.py`
- [ ] Протестирован User App
- [ ] Протестирован Admin App
- [ ] Настроена синхронизация отчетов
- [ ] Протестирована синхронизация
- [ ] Google Sheets настроены как read-only

---

## 🎉 **РЕЗУЛЬТАТ:**

**После миграции:**

- ⚡ **30x быстрее** запросы
- ⚡ **200x быстрее** массовые операции
- ✅ **Нет превышения лимитов**
- ✅ **Real-time обновления**
- ✅ **Масштабируемость** до 1000+ пользователей
- ✅ **Стоимость: $0/месяц**
- ✅ **Google Sheets для отчетов** (удобный просмотр)

---

## 📚 **ДОПОЛНИТЕЛЬНО:**

### **Документация:**
- Supabase Docs: https://supabase.com/docs
- PostgreSQL Docs: https://www.postgresql.org/docs/

### **Поддержка:**
- Supabase Discord: https://discord.supabase.com
- GitHub Issues: https://github.com/supabase/supabase

---

**МИГРАЦИЯ НА SUPABASE = МАКСИМАЛЬНАЯ ПРОИЗВОДИТЕЛЬНОСТЬ! 🚀**

**Вопросы? Смотрите документацию или пишите в поддержку!**
