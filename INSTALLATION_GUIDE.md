# 📦 Инструкция по установке WorkTimeTracker на ПК

## Требования

- **Windows 10/11** (или Linux/Mac)
- **Python 3.10 или выше**
- **Интернет-соединение** для установки зависимостей

---

## 🚀 Быстрая установка (Windows)

### Шаг 1: Установка Python

1. Скачайте Python с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно отметьте** "Add Python to PATH"
3. Проверьте установку:
   ```cmd
   python --version
   ```
   Должно показать версию Python 3.10 или выше

### Шаг 2: Клонирование/скачивание проекта

Если проект в Git:
```cmd
git clone <repository_url>
cd WorkTimeTracker
```

Или распакуйте архив с проектом в папку, например: `C:\WorkTimeTracker`

### Шаг 3: Создание виртуального окружения

Откройте командную строку в папке проекта:
```cmd
cd C:\WorkTimeTracker
python -m venv .venv
```

### Шаг 4: Активация виртуального окружения

```cmd
.venv\Scripts\activate
```

После активации в начале строки появится `(.venv)`

### Шаг 5: Установка зависимостей

```cmd
pip install -r requirements.txt
```

Если используете Supabase (рекомендуется):
```cmd
pip install supabase
```

### Шаг 6: Настройка конфигурации

#### Вариант A: Использование Supabase (рекомендуется)

1. Создайте файл `.env` в корне проекта:
   ```cmd
   copy .env.example .env
   ```

2. Откройте `.env` в текстовом редакторе и заполните:
   ```env
   # Supabase настройки (ОБЯЗАТЕЛЬНО)
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_KEY=your-supabase-anon-key
   
   # Telegram бот (ОБЯЗАТЕЛЬНО)
   TELEGRAM_TOKEN=your_bot_token_here
   TELEGRAM_MONITORING_CHAT_ID=your_chat_id_here
   ```

#### Вариант B: Использование Google Sheets

1. Создайте файл `.env`:
   ```cmd
   copy .env.example .env
   ```

2. Получите Google Service Account:
   - Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
   - Создайте проект или выберите существующий
   - Включите Google Sheets API
   - Создайте Service Account и скачайте JSON файл

3. Сохраните JSON файл в папку `credentials/service_account.json`

4. Откройте `.env` и заполните:
   ```env
   SPREADSHEET_ID=your_google_sheet_id
   GOOGLE_CREDENTIALS_FILE=credentials/service_account.json
   TELEGRAM_TOKEN=your_bot_token_here
   TELEGRAM_MONITORING_CHAT_ID=your_chat_id_here
   ```

### Шаг 7: Применение миграций базы данных

Если используете локальную базу данных:
```cmd
python db_migrations_improved.py local_backup.db migrate
```

---

## 🎯 Запуск приложения

### Для обычных пользователей:

**Способ 1: Через bat-файл (Windows)**
```cmd
run_user.bat
```

**Способ 2: Через командную строку**
```cmd
.venv\Scripts\activate
python user_app/main.py
```

### Для администраторов:

**Способ 1: Через bat-файл (Windows)**
```cmd
run_admin.bat
```

**Способ 2: Через командную строку**
```cmd
.venv\Scripts\activate
python admin_app/main_admin.py
```

---

## ✅ Проверка установки

Запустите диагностику:
```cmd
python tools/doctor.py
```

Должны увидеть:
- ✓ Конфигурация успешно проверена
- ✓ Учетные данные доступны
- ✓ База данных подключена

---

## 🔧 Решение проблем

### Проблема: "Python not found"
**Решение:** Убедитесь, что Python добавлен в PATH, или используйте полный путь:
```cmd
C:\Python310\python.exe --version
```

### Проблема: "Module not found"
**Решение:** Убедитесь, что виртуальное окружение активировано и зависимости установлены:
```cmd
.venv\Scripts\activate
pip install -r requirements.txt
```

### Проблема: "Credentials not found"
**Решение:** Проверьте файл `.env` и убедитесь, что все пути указаны правильно

### Проблема: "Cannot connect to database"
**Решение:** 
- Проверьте настройки Supabase в `.env`
- Или убедитесь, что файл `local_backup.db` существует

---

## 📝 Дополнительные настройки

### Создание ярлыка на рабочем столе (Windows)

1. Правой кнопкой на `run_user.bat` → "Создать ярлык"
2. Переместите ярлык на рабочий стол
3. Правой кнопкой на ярлык → "Свойства" → "Сменить значок" (опционально)

### Автозапуск при входе в Windows

1. Нажмите `Win + R`, введите `shell:startup`
2. Скопируйте ярлык `run_user.bat` в открывшуюся папку

---

## 🆘 Получение помощи

Если что-то не работает:
1. Проверьте логи в папке `logs/`
2. Запустите диагностику: `python tools/doctor.py`
3. Проверьте файл `.env` на наличие всех обязательных параметров

---

## 📚 Дополнительная документация

- `README.md` - Общая информация о проекте
- `QUICKSTART_MIGRATION.md` - Миграция на Supabase
- `SUPABASE_MIGRATION_GUIDE.md` - Подробное руководство по миграции
