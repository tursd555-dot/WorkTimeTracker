# 📊 Анализ проекта WorkTimeTracker

## 🎯 Общее описание

**WorkTimeTracker** — система отслеживания рабочего времени с поддержкой многопользовательского режима, интеграцией с Google Sheets/Supabase и Telegram-уведомлениями.

### Основные характеристики:
- **Язык**: Python 3.10+
- **GUI Framework**: PyQt5
- **База данных**: SQLite (локальная) + Google Sheets/Supabase (удаленная)
- **Архитектура**: Offline-first с автоматической синхронизацией
- **Масштабируемость**: Готова для работы с 200+ пользователями

---

## 🏗️ Архитектура проекта

### Структура приложений

#### 1. **User App** (`user_app/`)
Приложение для сотрудников:
- **Точка входа**: `main.py`
- **Основные компоненты**:
  - `gui.py` - главное окно приложения
  - `login_window.py` - окно авторизации
  - `db_local.py` - работа с локальной SQLite БД
  - `session.py` - управление сессиями пользователей
  - `api.py` - клиент для работы с API
  - `break_info_widget.py` - виджет отображения перерывов
  - `personal_rules.py` - персональные правила уведомлений

**Особенности**:
- Offline-first архитектура (работает без интернета)
- Автоматическая синхронизация в фоне
- Система отказоустойчивости (Health Checker, Degradation Manager)
- Поддержка незавершенных сессий

#### 2. **Admin App** (`admin_app/`)
Административная панель:
- **Точка входа**: `main_admin.py`
- **Основные компоненты**:
  - `gui_admin.py` - главное окно админки
  - `repo.py` - репозиторий данных
  - `break_manager.py` - управление графиками перерывов
  - `break_analytics_tab.py` - аналитика по перерывам
  - `break_schedule_dialog.py` - диалог создания графиков
  - `notifications_panel.py` - панель уведомлений
  - `audit_logger.py` - аудит действий

**Функционал**:
- Управление пользователями (CRUD)
- Просмотр и редактирование графика смен
- Управление шаблонами перерывов
- Назначение графиков сотрудникам
- Просмотр нарушений
- Аналитика по перерывам
- Принудительный разлогин пользователей

#### 3. **Telegram Bot** (`telegram_bot/`)
Бот для уведомлений:
- **Точка входа**: `main.py`
- **Функционал**:
  - Привязка Telegram chat_id к email пользователя
  - Отправка уведомлений о перерывах, нарушениях
  - Служебные уведомления администратору

---

## 🔄 Система синхронизации

### Компоненты синхронизации

#### 1. **SyncManager** (`auto_sync.py`)
Менеджер фоновой синхронизации:
- **Режимы работы**:
  - `ONLINE`: синхронизация каждые 10 секунд
  - `OFFLINE_RECOVERY`: синхронизация каждые 5 секунд при восстановлении
- **Особенности**:
  - Автоматическое определение состояния сети
  - Экспоненциальный backoff при ошибках
  - Batch-операции для оптимизации
  - Приоритетная обработка критических событий (LOGIN/LOGOUT)

#### 2. **ImprovedSyncQueue** (`sync/sync_queue_improved.py`)
Улучшенная очередь синхронизации:
- **Возможности**:
  - Batch-операции (до 20 записей за раз)
  - Приоритеты: CRITICAL > HIGH > NORMAL > LOW
  - Exponential backoff с jitter
  - Автоматическое разрешение конфликтов
  - Детальная телеметрия

#### 3. **ConflictResolver** (`sync/conflict_resolver.py`)
Разрешение конфликтов синхронизации:
- **Стратегии**:
  - `LAST_WRITE_WINS` - последняя запись побеждает
  - `ADMIN_WINS` - данные админа имеют приоритет
  - `USER_WINS` - данные пользователя имеют приоритет
  - `MERGE` - объединение данных

#### 4. **Network Utilities** (`sync/network.py`)
Утилиты для проверки сети:
- Быстрая проверка доступности интернета
- Определение состояния подключения

---

## 🗄️ Работа с данными

### Локальная база данных (`user_app/db_local.py`)

**Особенности**:
- SQLite с WAL-режимом для конкурентного доступа
- Единое соединение на процесс (singleton)
- Автоматические миграции
- Резервный путь при недоступности основного

**Таблицы**:
- `logs` - журнал событий (статусы, логины, логи)
- `sessions` - активные сессии пользователей
- `sync_queue` - очередь синхронизации
- `notifications` - уведомления

### Удаленное хранилище

#### Google Sheets API (`sheets_api_integration.py`)
- Интеграция с Google Sheets через Service Account
- Rate limiting защита
- Batch-операции для оптимизации

#### Supabase API (`supabase_api.py`)
- Альтернативный бэкенд (переключение через `USE_SUPABASE`)
- REST API для работы с данными
- Поддержка Row Level Security (RLS)

#### API Adapter (`api_adapter.py`)
Унифицированный интерфейс для переключения между бэкендами:
- Прозрачное переключение между Sheets и Supabase
- Единый API для приложений

---

## 🛡️ Системы отказоустойчивости

### 1. **Health Checker** (`shared/health/health_checker.py`)
Мониторинг состояния системы:
- **Проверки**:
  - База данных
  - Google Sheets API
  - Telegram API
  - Интернет-соединение
  - Дисковое пространство
  - Память
  - Очередь синхронизации

- **Метрики**:
  - Количество успешных/неуспешных проверок
  - Время последней проверки
  - Общий статус компонента

### 2. **Circuit Breaker** (`shared/resilience/circuit_breaker.py`)
Защита от каскадных сбоев:
- **Состояния**:
  - `CLOSED` - нормальная работа
  - `OPEN` - сервис недоступен (блокировка запросов)
  - `HALF_OPEN` - тестирование восстановления

- **Параметры**:
  - `failure_threshold` - порог ошибок для открытия
  - `recovery_timeout` - время до попытки восстановления
  - `success_threshold` - количество успехов для закрытия

### 3. **Degradation Manager** (`shared/resilience/degradation_manager.py`)
Управление режимами работы системы:
- **Режимы**:
  - `FULL` - полный функционал
  - `DEGRADED` - ограниченный функционал
  - `OFFLINE` - только локальная работа
  - `EMERGENCY` - только просмотр

- **Автоматическое переключение** на основе Health Checker

---

## 🔔 Система уведомлений

### Компоненты

#### 1. **Notification Engine** (`notifications/engine.py`)
Движок обработки уведомлений:
- Фоновый опросчик правил
- Обработка персональных правил
- Обработка служебных алертов

#### 2. **Rules Manager** (`notifications/rules_manager.py`)
Управление правилами уведомлений:
- Персональные правила для пользователей
- Служебные правила для администратора

#### 3. **Break Notifications** (`shared/break_notifications.py`)
Уведомления о перерывах:
- Превышение лимита времени
- Превышение количества перерывов
- Напоминания о возврате

---

## ⏱️ Система перерывов

### Компоненты

#### 1. **BreakManager** (`admin_app/break_manager.py`)
Управление графиками перерывов:
- Создание/редактирование шаблонов графиков
- Назначение графиков сотрудникам
- Отслеживание использования перерывов
- Выявление нарушений

#### 2. **Break Status Integration** (`shared/break_status_integration.py`)
Интеграция с системой статусов:
- Автоматический старт/завершение перерывов
- Отслеживание активных перерывов
- Обработка логина/логаута

#### 3. **Break Analytics** (`admin_app/break_analytics_tab.py`)
Аналитика по перерывам:
- Статистика использования
- Отчеты по нарушениям
- Визуализация данных

---

## 🔐 Безопасность

### Управление учетными данными (`config.py`)

**Режимы хранения**:
1. **Зашифрованный ZIP** (рекомендуется для production):
   - Файл `secret_creds.zip` рядом с EXE
   - Пароль в переменной окружения `CREDENTIALS_ZIP_PASSWORD`
   - AES-256 шифрование

2. **JSON файл** (для разработки):
   - Путь в `GOOGLE_CREDENTIALS_FILE`
   - Может быть относительным или абсолютным

**Особенности**:
- Ленивая загрузка credentials
- Кэширование в памяти (TTL 1 час)
- Автоматическая очистка временных файлов

### Аудит (`admin_app/audit_logger.py`)
Логирование действий администратора:
- Создание/изменение/удаление пользователей
- Изменение графиков
- Принудительный разлогин

---

## 📦 Дополнительные модули

### Shared модули (`shared/`)

#### 1. **Data Cache** (`shared/data_cache.py`)
Кэширование данных:
- TTL-based кэш
- Кэширование списка пользователей
- Rate limiting для API вызовов

#### 2. **Sheets Batching** (`shared/sheets_batching.py`)
Batch-операции для Google Sheets:
- Группировка операций
- Оптимизация запросов
- Context manager для автоматического выполнения

#### 3. **Connection Pool** (`shared/db/connection_pool.py`)
Пул соединений с БД:
- Переиспользование соединений
- Управление жизненным циклом
- Статистика использования

#### 4. **Encrypted Database** (`shared/db/encrypted_database.py`)
Поддержка SQLCipher:
- Шифрование БД на уровне SQLite
- Миграция с незашифрованной БД
- Ротация ключей

---

## 🛠️ Инструменты разработки

### Tools (`tools/`)

1. **wtt_setup.py** - мастер настройки проекта
2. **doctor.py** - диагностика системы
3. **sheets_bootstrap.py** - инициализация Google Sheets
4. **tg_envcheck.py** - проверка Telegram конфигурации
5. **tg_send.py** - тестирование отправки уведомлений

### Dev-tools (`dev-tools/`)

- **diagnostic/** - диагностические скрипты
- **fix/** - скрипты исправления проблем
- **test/** - тестовые утилиты
- **build/** - конфигурации PyInstaller

---

## 📊 Конфигурация

### Основные настройки (`config.py`)

**Синхронизация**:
- `SYNC_INTERVAL_ONLINE`: 10 секунд
- `SYNC_INTERVAL_OFFLINE_RECOVERY`: 5 секунд
- `SYNC_BATCH_SIZE`: 50 записей
- `SYNC_RETRY_STRATEGY`: [60, 300, 900, 1800, 3600] секунд

**Лимиты**:
- `BREAK_LIMIT_MINUTES`: 15 минут
- `LUNCH_LIMIT_MINUTES`: 60 минут
- `GOOGLE_API_LIMITS`: 60 запросов/минуту

**Уведомления**:
- `PERSONAL_RULES_ENABLED`: True
- `SERVICE_ALERTS_ENABLED`: True
- `TELEGRAM_MIN_INTERVAL_SEC`: 600 секунд

---

## 🔍 Выявленные проблемы и рекомендации

### ✅ Сильные стороны

1. **Отказоустойчивость**:
   - Circuit Breaker для защиты от сбоев
   - Health Checker для мониторинга
   - Degradation Manager для адаптации

2. **Производительность**:
   - Batch-операции для синхронизации
   - Кэширование данных
   - Оптимизация запросов к БД

3. **Безопасность**:
   - Шифрование credentials
   - Аудит действий
   - Поддержка SQLCipher

4. **Масштабируемость**:
   - Offline-first архитектура
   - Поддержка 200+ пользователей
   - Эффективная синхронизация

### ⚠️ Потенциальные проблемы

1. **Дублирование кода**:
   - Несколько версий `break_notifications.py` (backup, fixed)
   - Дублирование логики в разных модулях

2. **Зависимости**:
   - Жесткая привязка к PyQt5 (может усложнить тестирование)
   - Множественные импорты в разных местах

3. **Документация**:
   - Недостаточно комментариев в некоторых модулях
   - Отсутствие API документации

4. **Тестирование**:
   - Нет видимых unit-тестов
   - Нет интеграционных тестов

### 💡 Рекомендации по улучшению

1. **Рефакторинг**:
   - Удалить дублирующиеся файлы
   - Вынести общую логику в shared модули
   - Унифицировать обработку ошибок

2. **Тестирование**:
   - Добавить unit-тесты для критичных модулей
   - Интеграционные тесты для синхронизации
   - Тесты для отказоустойчивости

3. **Документация**:
   - Добавить docstrings для всех публичных методов
   - Создать архитектурную диаграмму
   - Документировать API endpoints

4. **Мониторинг**:
   - Добавить метрики Prometheus
   - Интеграция с системами логирования (ELK, Loki)
   - Дашборды для визуализации метрик

---

## 📈 Метрики проекта

- **Строк кода**: ~15,000+ (оценка)
- **Модулей**: 53 production файла
- **Зависимостей**: PyQt5, gspread, requests, python-dateutil
- **База данных**: SQLite (локальная) + Google Sheets/Supabase (удаленная)

---

## 🎯 Заключение

Проект **WorkTimeTracker** представляет собой хорошо структурированную систему с продуманной архитектурой отказоустойчивости. Основные преимущества:

- ✅ Offline-first подход
- ✅ Системы мониторинга и защиты от сбоев
- ✅ Масштабируемость
- ✅ Безопасность данных

Области для улучшения:
- 📝 Документация
- 🧪 Тестирование
- 🔧 Рефакторинг дублирующегося кода

Проект готов к использованию в production, но рекомендуется провести рефакторинг и добавить тесты перед масштабированием.
