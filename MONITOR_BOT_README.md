# Monitor Bot 24/7 - Инструкция по использованию

## Описание

Monitor Bot - это Telegram бот, который работает 24/7 и автоматически мониторит Supabase на наличие:
- **Новых нарушений** правил перерывов
- **Активных перерывов** с превышением лимитов времени

Бот отправляет уведомления в Telegram группу мониторинга без необходимости держать открытыми приложения.

## Два режима работы бота

### 1. Linker Bot (по умолчанию)
- Привязывает email сотрудников к Telegram chat_id
- Запускается через `bot_launcher.py` без флагов

### 2. Monitor Bot (24/7)
- Мониторит Supabase на нарушения и перерывы
- Отправляет уведомления в Telegram
- Работает постоянно в фоновом режиме

## Запуск Monitor Bot

### Вариант 1: Через bot_launcher.py (GUI)

```powershell
# Запуск Monitor Bot через GUI
python bot_launcher.py --monitor
```

Или если собран exe:
```powershell
WorkTimeTracker_Bot.exe --monitor
```

### Вариант 2: Напрямую (консольный режим)

```powershell
# Установить режим через переменную окружения
$env:BOT_MODE = "monitor"
python telegram_bot/main.py --monitor

# Или напрямую
python telegram_bot/monitor_bot.py
```

### Вариант 3: Как Windows сервис (рекомендуется для 24/7)

1. Используйте NSSM (Non-Sucking Service Manager) или Task Scheduler
2. Создайте задачу, которая запускает:
   ```
   python telegram_bot/monitor_bot.py
   ```
3. Настройте автозапуск при старте Windows

## Настройка

### Требования

1. **Переменные окружения в `.env`:**
   ```env
   TELEGRAM_BOT_TOKEN=your_bot_token
   TELEGRAM_MONITORING_CHAT_ID=-1001234567890
   TELEGRAM_ADMIN_CHAT_ID=123456789
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_KEY=your_supabase_key
   ```

2. **Таблицы в Supabase:**
   - `violations` - таблица нарушений
   - `break_log` - таблица перерывов
   - `users` - таблица пользователей

### Интервалы проверки

По умолчанию:
- **Нарушения**: проверка каждые 60 секунд
- **Перерывы**: проверка каждые 30 секунд
- **Общий цикл**: каждые 30 секунд

Можно изменить в `telegram_bot/monitor_bot.py`:
```python
CHECK_INTERVAL = 30  # Общий интервал
VIOLATIONS_CHECK_INTERVAL = 60  # Нарушения
BREAKS_CHECK_INTERVAL = 30  # Перерывы
```

## Что мониторит бот

### 1. Нарушения правил перерывов

Бот проверяет таблицу `violations` и отправляет уведомления о новых нарушениях:
- **OUT_OF_WINDOW** - Вне временного окна
- **OVER_LIMIT** - Превышен лимит времени
- **QUOTA_EXCEEDED** - Превышено количество перерывов

**Формат уведомления:**
```
⚠️ НАРУШЕНИЕ ПРАВИЛ ПЕРЕРЫВОВ

Сотрудник: user@example.com
Тип: Превышен лимит времени
Время: 2025-12-26 15:30:00
Детали: ...
```

### 2. Активные перерывы с превышением лимитов

Бот проверяет активные перерывы (без `end_time`) и отправляет предупреждения:
- **Перерыв**: лимит 15 минут
- **Обед**: лимит 60 минут

**Дебаунсинг**: уведомления отправляются не чаще раза в 5 минут для одного сотрудника.

**Формат уведомления:**
```
⏰ ПРЕВЫШЕНИЕ ЛИМИТА ПЕРЕРЫВА

Сотрудник: user@example.com
Тип: Перерыв
Длительность: 20 мин (лимит 15 мин)
Превышение: +5 мин
Время: 15:30:00
```

## Логирование

Логи сохраняются в:
- `%APPDATA%\WorkTimeTracker\logs\wtt-monitor-bot.log` (Windows)
- `~/.local/share/WorkTimeTracker/logs/wtt-monitor-bot.log` (Linux)

## Автозапуск при старте Windows

### Способ 1: Task Scheduler

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте новую задачу:
   - **Триггер**: При запуске компьютера
   - **Действие**: Запустить программу
   - **Программа**: `python` (или путь к python.exe)
   - **Аргументы**: `telegram_bot/monitor_bot.py`
   - **Рабочая папка**: путь к проекту
   - **Параметры**: Запускать от имени пользователя, даже если пользователь не вошел в систему

### Способ 2: NSSM (Non-Sucking Service Manager)

1. Скачайте NSSM: https://nssm.cc/download
2. Установите сервис:
   ```powershell
   nssm install WorkTimeTrackerMonitorBot
   ```
3. В настройках укажите:
   - **Path**: путь к `python.exe`
   - **Arguments**: `telegram_bot/monitor_bot.py`
   - **Startup directory**: путь к проекту
4. Запустите сервис:
   ```powershell
   nssm start WorkTimeTrackerMonitorBot
   ```

## Остановка бота

### Через GUI (bot_launcher.py)
Нажмите кнопку **"🛑 Остановить"**

### Через консоль
Нажмите `Ctrl+C` в консоли, где запущен бот

### Через Task Scheduler
Остановите задачу в планировщике

## Проверка работы

1. **Проверьте логи** - должны быть записи о проверках
2. **Отправьте тестовое нарушение** в Supabase таблицу `violations`
3. **Проверьте Telegram** - должно прийти уведомление

## Устранение проблем

### Бот не запускается
- Проверьте наличие всех переменных окружения в `.env`
- Проверьте логи в `%APPDATA%\WorkTimeTracker\logs\`
- Убедитесь, что Supabase доступен

### Уведомления не приходят
- Проверьте `TELEGRAM_MONITORING_CHAT_ID` в `.env`
- Проверьте, что бот имеет доступ к таблицам Supabase
- Проверьте логи на наличие ошибок

### Бот останавливается
- Проверьте логи на ошибки
- Убедитесь, что интернет-соединение стабильно
- Проверьте доступность Supabase API

## Преимущества Monitor Bot

✅ **Работает 24/7** без необходимости держать открытыми приложения  
✅ **Автоматический мониторинг** нарушений и перерывов  
✅ **Мгновенные уведомления** в Telegram  
✅ **Независим от приложений** - работает отдельно  
✅ **Легко настроить** автозапуск при старте Windows  

## Сравнение с обычными уведомлениями

| | Обычные уведомления | Monitor Bot |
|---|---|---|
| Требует открытое приложение | ✅ Да | ❌ Нет |
| Работает 24/7 | ❌ Нет | ✅ Да |
| Мониторинг нарушений | ✅ Да | ✅ Да |
| Мониторинг перерывов | ✅ Да | ✅ Да |
| Автозапуск | ❌ Сложно | ✅ Легко |
