# Руководство по автоматической архивации данных

Система автоматической архивации позволяет экономить место в Supabase на бесплатном тарифе, автоматически перенося старые данные в Google Таблицы.

## Как это работает

1. **Выбор данных**: Система находит все записи старше указанного возраста (по умолчанию 90 дней)
2. **Экспорт в Google Sheets**: Данные экспортируются в отдельные листы архива в Google Таблице
3. **Удаление из Supabase**: После успешного экспорта данные удаляются из Supabase (если включено)

## Настройка

### Переменные окружения

Добавьте в ваш `.env` файл:

```bash
# Возраст данных для архивации (дни)
ARCHIVE_AGE_DAYS=90

# Удалять ли данные из Supabase после архивации (1 = да, 0 = нет)
ARCHIVE_DELETE_AFTER=1

# Размер батча для экспорта
ARCHIVE_BATCH_SIZE=1000

# ID Google Таблицы для архива (если не указан, используется GOOGLE_SHEET_ID)
GOOGLE_ARCHIVE_SHEET_ID=your_archive_sheet_id_here
```

### Таблицы для архивации

По умолчанию архивируются следующие таблицы:
- `work_log` - детальный лог всех действий
- `break_log` - лог перерывов
- `work_sessions` - рабочие сессии
- `violations` - нарушения
- `sync_log` - лог синхронизации

## Использование

### Ручной запуск

**Windows:**
```cmd
python run_archive.py
```
или просто:
```cmd
run_archive.bat
```

**Linux/Mac:**
```bash
python3 run_archive.py
```

### Автоматический запуск (Linux/Mac)

Добавьте в crontab для ежедневного запуска в 2:00 ночи:

```bash
crontab -e
```

Добавьте строку:
```bash
0 2 * * * /path/to/project/scripts/archive_daily.sh
```

### Автоматический запуск (Windows)

**Вариант 1: Через Task Scheduler (рекомендуется)**
1. Откройте Task Scheduler (Планировщик задач)
2. Создайте новую задачу
3. Установите триггер: ежедневно в 2:00
4. Действие: запустить программу `C:\path\to\project\run_archive.bat`
5. Или запустить `python.exe` с аргументом `C:\path\to\project\run_archive.py`

**Вариант 2: Через bat-файл в планировщике**
Просто укажите полный путь к `run_archive.bat` в планировщике задач

## Структура архивных листов

В Google Таблице создаются листы с префиксом `Archive_`:
- `Archive_work_log` - архив логов действий
- `Archive_break_log` - архив перерывов
- `Archive_work_sessions` - архив сессий
- `Archive_violations` - архив нарушений
- `Archive_sync_log` - архив синхронизации

## Логирование

Логи архивации сохраняются в:
- Консоль (stdout)
- Файл `archive.log` в корне проекта
- Для cron задач: `logs/archive_YYYYMMDD.log`

## Безопасность

⚠️ **Важно**: 
- Перед первым запуском убедитесь, что у вас есть резервная копия данных
- Рекомендуется сначала запустить с `ARCHIVE_DELETE_AFTER=0` для проверки
- Проверьте, что данные корректно экспортируются в Google Sheets перед включением удаления

## Мониторинг

После каждого запуска выводится статистика:
- Количество записей найдено
- Количество успешно архивировано
- Количество удалено из Supabase
- Количество ошибок
- Время выполнения

## Пример вывода

```
🔄 Starting archive for table 'work_log'
Found 15234 old records in 'work_log' (older than 2024-09-15)
Exported batch 1/16 (1000 rows) to 'Archive_work_log'
...
✅ Successfully exported 15234 records to 'Archive_work_log'
✅ Deleted 15234 records from 'work_log'
✅ Archive completed for 'work_log': 15234 archived, 15234 deleted, 0 failed (45.2s)

🎉 Archive process completed: 15234 archived, 15234 deleted, 0 failed
```

## Устранение неполадок

### Ошибка: "GOOGLE_ARCHIVE_SHEET_ID not set"
Установите переменную окружения `GOOGLE_ARCHIVE_SHEET_ID` или `GOOGLE_SHEET_ID`

### Ошибка: "Failed to export records"
- Проверьте доступность Google Sheets API
- Убедитесь, что service account имеет доступ к таблице
- Проверьте квоты Google Sheets API

### Данные не удаляются
- Проверьте значение `ARCHIVE_DELETE_AFTER` (должно быть `1`)
- Проверьте логи на наличие ошибок экспорта

## Рекомендации

1. **Частота архивации**: Рекомендуется запускать раз в день в нерабочее время
2. **Возраст данных**: 90 дней - хороший баланс между экономией места и доступностью данных
3. **Мониторинг**: Регулярно проверяйте логи и статистику архивации
4. **Резервные копии**: Делайте резервные копии перед первым запуском

## Ограничения

- Бесплатный тариф Supabase: 500 MB базы данных
- Google Sheets: до 10 миллионов ячеек на лист
- Рекомендуется архивировать данные старше 90 дней для оптимального использования места
