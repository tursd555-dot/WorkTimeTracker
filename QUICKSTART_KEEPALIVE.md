# ⚡ Быстрый старт: Supabase Keep-Alive

**Предотвращение приостановки Supabase проекта на Free Tier за 5 минут**

---

## ❓ Зачем это нужно?

Supabase приостанавливает неактивные проекты через **7 дней** на бесплатном тарифе.

**Решение:** Автоматический "пинг" каждые 3 дня.

---

## 🚀 Рекомендуемый способ: GitHub Actions

**Преимущества:** Бесплатно, автоматически, 24/7, не требует локального ПК

### Шаг 1: Загрузите проект на GitHub (если еще не загружен)

```bash
git add .
git commit -m "Add Supabase keep-alive"
git push
```

### Шаг 2: Добавьте Secrets

1. Откройте репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**
4. Добавьте два секрета:

**Секрет 1:**
- Name: `SUPABASE_URL`
- Value: `https://ваш-проект.supabase.co`

**Секрет 2:**
- Name: `SUPABASE_KEY`
- Value: `ваш-anon-key` (из Supabase Dashboard → Settings → API)

### Шаг 3: Готово! ✅

Workflow автоматически запустится каждые 3 дня.

**Проверить:** Actions → Supabase Keep-Alive → Run workflow (ручной тест)

---

## 💻 Альтернатива: Локальный планировщик

### Windows

```powershell
# Тест
pip install supabase
python supabase_keepalive.py

# Автоматизация
# Откройте Планировщик задач (taskschd.msc)
# Создайте задачу: запуск run_keepalive.bat каждые 3 дня
```

### Linux/Mac

```bash
# Тест
pip3 install supabase
./run_keepalive.sh

# Автоматизация (cron)
crontab -e
# Добавьте строку:
# 0 3 */3 * * /path/to/WorkTimeTracker/run_keepalive.sh
```

---

## 📚 Подробная документация

См. [SUPABASE_KEEPALIVE_GUIDE.md](SUPABASE_KEEPALIVE_GUIDE.md)

---

## ✅ Проверка

### Проверить статус

```bash
python supabase_keepalive.py
```

Ожидаемый вывод:
```
✅ Supabase активен! Запрос выполнен успешно
```

### Проверить Supabase Dashboard

1. Откройте https://app.supabase.com
2. **Settings** → **General**
3. **Last Activity** - должна быть недавняя дата

---

## 🛠️ Проблемы?

**❌ "SUPABASE_URL и SUPABASE_KEY должны быть установлены"**
- Создайте файл `.env` с этими переменными
- Для GitHub Actions: добавьте Secrets

**❌ "supabase library not installed"**
- Запустите: `pip install supabase`

**Другие проблемы:** См. [SUPABASE_KEEPALIVE_GUIDE.md](SUPABASE_KEEPALIVE_GUIDE.md) → раздел "Устранение проблем"

---

**🎉 Готово! Ваш Supabase теперь всегда активен!**
