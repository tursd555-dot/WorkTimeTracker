# 🔄 Supabase Keep-Alive - Руководство

**Автоматическое поддержание активности Supabase проекта на бесплатном тарифе**

---

## 📋 Содержание

1. [Зачем это нужно?](#зачем-это-нужно)
2. [Варианты решения](#варианты-решения)
3. [Вариант 1: GitHub Actions (рекомендуется)](#вариант-1-github-actions)
4. [Вариант 2: Локальный планировщик (Windows)](#вариант-2-windows-task-scheduler)
5. [Вариант 3: Cron (Linux/Mac)](#вариант-3-cron-linuxmac)
6. [Вариант 4: Внешние сервисы](#вариант-4-внешние-сервисы)
7. [Проверка работы](#проверка-работы)
8. [Устранение проблем](#устранение-проблем)

---

## ❓ Зачем это нужно?

### Проблема
Supabase на бесплатном тарифе (Free Tier) **автоматически приостанавливает проекты** через **7 дней неактивности**.

После приостановки:
- ❌ База данных недоступна
- ❌ API не отвечает
- ❌ Приложение не работает

### Решение
Регулярно выполнять **легкие запросы к БД** (каждые 3 дня), чтобы Supabase считал проект активным.

---

## 🎯 Варианты решения

| Вариант | Преимущества | Недостатки | Рекомендация |
|---------|-------------|-----------|--------------|
| **GitHub Actions** | ✅ Бесплатно<br>✅ Автоматически<br>✅ Надежно<br>✅ Не требует локального ПК | ❌ Нужен GitHub репозиторий | ⭐ **Лучший вариант!** |
| **Windows Task Scheduler** | ✅ Локальный контроль<br>✅ Без интернет сервисов | ❌ Требует постоянно включенный ПК<br>❌ Ручная настройка | Если нет GitHub |
| **Linux/Mac Cron** | ✅ Локальный контроль<br>✅ Легкая настройка | ❌ Требует постоянно включенный сервер | Если есть VPS/сервер |
| **Внешние сервисы** | ✅ Бесплатно<br>✅ Автоматически | ❌ Зависимость от третьих сторон | Альтернатива |

---

## 🚀 Вариант 1: GitHub Actions (рекомендуется)

**Идеально для большинства пользователей!**

### Преимущества:
- ✅ Полностью бесплатно
- ✅ Работает автоматически 24/7
- ✅ Не требует включенного компьютера
- ✅ Надежно и стабильно
- ✅ Уже настроено в проекте!

### Шаги по настройке:

#### 1. Загрузите проект на GitHub

```bash
# Если еще не загружен
git init
git add .
git commit -m "Initial commit with Supabase keep-alive"
git branch -M main
git remote add origin https://github.com/ваш-username/WorkTimeTracker.git
git push -u origin main
```

#### 2. Добавьте Secrets в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте два секрета:

**Секрет 1:**
- Name: `SUPABASE_URL`
- Value: `https://ваш-проект.supabase.co`

**Секрет 2:**
- Name: `SUPABASE_KEY`
- Value: `ваш-anon-key`

#### 3. Проверьте workflow файл

Файл уже создан: `.github/workflows/supabase-keepalive.yml`

```yaml
name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 0 */3 * *'  # Каждые 3 дня в 00:00 UTC
  workflow_dispatch:  # Ручной запуск

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install supabase
      - run: python supabase_keepalive.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
```

#### 4. Протестируйте

1. Перейдите в **Actions** вкладку на GitHub
2. Выберите workflow **Supabase Keep-Alive**
3. Нажмите **Run workflow** → **Run workflow**
4. Подождите ~30 секунд
5. Должно появиться ✅ зеленая галочка

#### 5. Готово!

🎉 **Теперь GitHub будет автоматически "пинговать" ваш Supabase каждые 3 дня!**

**Расписание:**
- Запуск каждые 3 дня (1-го, 4-го, 7-го, 10-го и т.д. числа)
- Время: 00:00 UTC (03:00 МСК)
- Можно запустить вручную в любое время

---

## 💻 Вариант 2: Windows Task Scheduler

**Если нужен локальный контроль или нет GitHub**

### Шаги по настройке:

#### 1. Установите зависимости

```powershell
pip install supabase python-dotenv
```

#### 2. Настройте .env файл

Создайте файл `.env` в корне проекта:

```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш-anon-key
```

#### 3. Протестируйте скрипт

Запустите вручную:

```powershell
python supabase_keepalive.py
```

Или двойным кликом: `run_keepalive.bat`

Должно появиться:
```
✅ Supabase активен! Запрос выполнен успешно
```

#### 4. Настройте Планировщик задач

**Способ A: Автоматически (PowerShell)**

Запустите PowerShell **от имени Администратора** и выполните:

```powershell
$action = New-ScheduledTaskAction -Execute "C:\путь\к\WorkTimeTracker\run_keepalive.bat"
$trigger = New-ScheduledTaskTrigger -Daily -At "03:00" -DaysInterval 3
$principal = New-ScheduledTaskPrincipal -UserId "$env:USERNAME" -RunLevel Highest
$settings = New-ScheduledTaskSettingsSet -AllowStartIfOnBatteries -DontStopIfGoingOnBatteries
Register-ScheduledTask -TaskName "Supabase Keep-Alive" -Action $action -Trigger $trigger -Principal $principal -Settings $settings
```

**Способ B: Вручную (GUI)**

1. Откройте **Планировщик заданий** (Task Scheduler)
   - Нажмите Win+R → `taskschd.msc` → Enter

2. **Создать простую задачу**
   - Правый клик на "Библиотека планировщика заданий" → "Создать задачу..."

3. **Вкладка "Общие":**
   - Имя: `Supabase Keep-Alive`
   - Описание: `Поддержание активности Supabase проекта`
   - ☑️ Выполнять вне зависимости от регистрации пользователя
   - ☑️ Выполнять с наивысшими правами

4. **Вкладка "Триггеры":**
   - Нажмите "Создать..."
   - Начать задачу: **По расписанию**
   - Параметры: **Ежедневно**
   - Повторять каждые: **3 дня**
   - Время: `03:00` (или любое удобное)
   - ✅ Включено

5. **Вкладка "Действия":**
   - Нажмите "Создать..."
   - Действие: **Запуск программы**
   - Программа: `C:\путь\к\WorkTimeTracker\run_keepalive.bat`

6. **Вкладка "Условия":**
   - ☑️ Запускать задачу при питании от батарей
   - ☑️ Не останавливать при переходе на батарею

7. **Вкладка "Параметры":**
   - ☑️ Разрешить запуск задачи по требованию

8. Нажмите **ОК**

#### 5. Протестируйте задачу

1. Найдите задачу в списке
2. Правый клик → **Выполнить**
3. Проверьте журнал выполнения

---

## 🐧 Вариант 3: Cron (Linux/Mac)

**Если у вас есть VPS или локальный Linux/Mac сервер**

### Шаги по настройке:

#### 1. Установите зависимости

```bash
pip3 install supabase python-dotenv
```

#### 2. Настройте .env файл

```bash
cd /path/to/WorkTimeTracker
nano .env
```

Добавьте:
```env
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_KEY=ваш-anon-key
```

#### 3. Протестируйте скрипт

```bash
chmod +x run_keepalive.sh
./run_keepalive.sh
```

Должно появиться:
```
✅ Supabase активен! Запрос выполнен успешно
```

#### 4. Настройте Cron

Откройте crontab:

```bash
crontab -e
```

Добавьте строку (запуск каждые 3 дня в 03:00):

```cron
0 3 */3 * * /path/to/WorkTimeTracker/run_keepalive.sh >> /path/to/WorkTimeTracker/keepalive.log 2>&1
```

Или более частый запуск (каждые 3 дня, любое время):

```cron
0 0 */3 * * /path/to/WorkTimeTracker/run_keepalive.sh >> /path/to/WorkTimeTracker/keepalive.log 2>&1
```

Сохраните и выйдите (Ctrl+X, Y, Enter).

#### 5. Проверьте Cron

Посмотреть список задач:

```bash
crontab -l
```

Проверить логи:

```bash
tail -f /path/to/WorkTimeTracker/keepalive.log
```

---

## 🌐 Вариант 4: Внешние сервисы

### A. UptimeRobot (бесплатно)

**Как работает:** Сервис пингует ваш URL каждые 5 минут.

**Проблема:** Нужен HTTP endpoint для пинга.

**Решение:** Создайте Supabase Edge Function:

1. В Supabase Dashboard: **Edge Functions** → **Create Function**
2. Имя: `keepalive`
3. Код:

```javascript
import { serve } from "https://deno.land/std@0.168.0/http/server.ts"

serve(async (req) => {
  // Простой запрос к БД
  const { createClient } = await import('https://esm.sh/@supabase/supabase-js@2')
  const supabaseUrl = Deno.env.get('SUPABASE_URL')!
  const supabaseKey = Deno.env.get('SUPABASE_ANON_KEY')!

  const supabase = createClient(supabaseUrl, supabaseKey)
  await supabase.from('users').select('id').limit(1)

  return new Response(JSON.stringify({ status: 'ok' }), {
    headers: { 'Content-Type': 'application/json' },
  })
})
```

4. Deploy
5. Получите URL: `https://ваш-проект.supabase.co/functions/v1/keepalive`
6. Зарегистрируйтесь на https://uptimerobot.com
7. Добавьте monitor:
   - Type: HTTP(s)
   - URL: `https://ваш-проект.supabase.co/functions/v1/keepalive`
   - Monitoring Interval: 5 minutes

### B. Cron-job.org (бесплатно)

1. Зарегистрируйтесь на https://cron-job.org
2. Создайте задание с вызовом Edge Function (как выше)
3. Расписание: каждые 3 дня

### C. EasyCron (бесплатно)

Аналогично cron-job.org: https://www.easycron.com

---

## ✅ Проверка работы

### 1. Проверить последнюю активность в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **Settings** → **General**
3. Найдите **Last Activity** - должна быть недавняя дата

### 2. Проверить логи GitHub Actions

1. Перейдите в **Actions** вкладку на GitHub
2. Выберите последний запуск **Supabase Keep-Alive**
3. Должен быть статус ✅ Success

### 3. Проверить локальные логи

**Windows:**
```powershell
Get-ScheduledTask -TaskName "Supabase Keep-Alive" | Get-ScheduledTaskInfo
```

**Linux/Mac:**
```bash
tail -20 /path/to/WorkTimeTracker/keepalive.log
```

### 4. Ручной тест

```bash
python supabase_keepalive.py
```

Должно появиться:
```
================================================================================
🔄 SUPABASE KEEP-ALIVE SCRIPT
================================================================================
✅ Загружены переменные из .env
🔌 Подключение к Supabase: https://xxx.supabase.co
✅ Supabase активен! Запрос выполнен успешно
   Время: 2024-01-15 12:30:45 UTC
================================================================================
✅ ГОТОВО! Supabase проект активен
   Следующий запуск: через 3 дня
================================================================================
```

---

## 🛠️ Устранение проблем

### ❌ Ошибка: "SUPABASE_URL и SUPABASE_KEY должны быть установлены"

**Причина:** Не найдены переменные окружения.

**Решение:**

1. Проверьте файл `.env` в корне проекта
2. Убедитесь что переменные установлены:
   ```env
   SUPABASE_URL=https://ваш-проект.supabase.co
   SUPABASE_KEY=ваш-anon-key
   ```
3. Для GitHub Actions: проверьте Secrets в настройках репозитория

### ❌ Ошибка: "supabase library not installed"

**Причина:** Не установлена библиотека.

**Решение:**

```bash
pip install supabase
# или
pip3 install supabase
```

### ❌ Ошибка: "Ошибка при выполнении запроса"

**Причина:** Неверные credentials или проблема с сетью.

**Решение:**

1. Проверьте URL и Key в Supabase Dashboard
2. Проверьте интернет соединение
3. Проверьте что проект Supabase активен (не удален)

### ❌ GitHub Actions не запускается

**Причина:** Workflow файл неправильно настроен или Secrets не установлены.

**Решение:**

1. Проверьте что файл `.github/workflows/supabase-keepalive.yml` существует
2. Проверьте Secrets в Settings → Secrets and variables → Actions
3. Попробуйте ручной запуск: Actions → Supabase Keep-Alive → Run workflow

### ❌ Проект все равно приостановлен

**Причина:** Keep-alive не запускался более 7 дней.

**Решение:**

1. Восстановите проект в Supabase Dashboard (кнопка "Restore")
2. Убедитесь что keep-alive работает правильно
3. Проверьте расписание запусков (должно быть каждые 3 дня)

---

## 📊 Мониторинг

### Опционально: Таблица для логирования

Создайте таблицу в Supabase для отслеживания keep-alive запусков:

```sql
CREATE TABLE keepalive_log (
    id BIGSERIAL PRIMARY KEY,
    timestamp TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    status TEXT NOT NULL,
    details TEXT
);

-- RLS политики (опционально)
ALTER TABLE keepalive_log ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Allow all operations" ON keepalive_log
FOR ALL USING (true);
```

Скрипт автоматически будет записывать туда логи.

### Уведомления

Если хотите получать уведомления при проблемах:

1. **GitHub Actions:** Можно настроить уведомления на email в GitHub Settings
2. **Telegram:** Добавьте отправку сообщения в скрипт при ошибке
3. **Email:** Используйте SMTP для отправки писем при сбоях

---

## 📝 Резюме

| Действие | Частота | Метод |
|----------|---------|-------|
| Автоматический keep-alive | Каждые 3 дня | GitHub Actions ⭐ |
| Ручная проверка | По необходимости | `python supabase_keepalive.py` |
| Проверка статуса | Раз в неделю | Supabase Dashboard |
| Резервная копия | Ежедневно | Автоматически в Supabase |

**Рекомендуемая конфигурация:**
- ✅ GitHub Actions для автоматизации
- ✅ Локальный скрипт для ручной проверки
- ✅ Мониторинг через Supabase Dashboard

---

## 🎉 Готово!

Теперь ваш Supabase проект будет оставаться активным **24/7** на бесплатном тарифе! 🚀

**Вопросы?** Проверьте секцию [Устранение проблем](#устранение-проблем) или откройте issue в GitHub репозитории.
